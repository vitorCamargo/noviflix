{
  "name": "noviflix-tmdb-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies TMDB, holding the API token server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put TMDB_ACCESS_TOKEN",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
